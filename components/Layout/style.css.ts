import { style } from "@vanilla-extract/css";

export const layout = style({
  position: "absolute",
  top: 0,
  left: 0,
  width: "100%",
  minHeight: "100%",
});

export const scrollContainer = style({});
